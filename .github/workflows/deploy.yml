name: Deploy Worker

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: deploy-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    name: 类型检查与单测
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 11

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: 安装依赖
        run: pnpm install --frozen-lockfile

      - name: 类型检查
        run: pnpm run typecheck

      - name: 单元测试
        run: pnpm run test

  deploy:
    name: 部署到 Cloudflare
    needs: verify
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 11

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: 安装依赖
        run: pnpm install --frozen-lockfile

      # secrets 块会把下面 env 里同名的值写入 Worker（等价于 wrangler secret put）。
      # 不想在 CI 里管密钥的话，删掉 secrets 与 env 这两段，改为本地手工
      # `pnpm wrangler secret put ...` 即可（密钥会一直保留在 Worker 上）。
      - name: 部署
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          packageManager: pnpm
          # ⚠️ 顺序至关重要：wrangler-action 的 secrets 块是在 deploy 之后执行的，
          # 这正好满足「先发代码、再传密钥」的要求；反过来会把 script-secret 绑定弄丢，
          # 线上表现为 500 MISSING_CONFIG（详见 README 第 2 节）。
          # --keep-vars 用于保住配置未声明的 vars。
          command: deploy --keep-vars
          secrets: |
            SUPABASE_SERVICE_ROLE_KEY
            SUPABASE_ANON_KEY
        env:
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}
